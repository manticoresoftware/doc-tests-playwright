{
  "name": "doc-tests-playwright",
  "version": "1.0.0",
  "description": "Playwright UI tests for Manticore Search documentation website",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:ui": "npx playwright test --ui",
    "test:docker": "docker compose run --rm playwright",
    "report": "npx playwright show-report",
    "assistant": "tsx assistant/server.ts",
    "assistant:dev": "tsx watch assistant/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.4.0",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/express": "^5.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.9.3"
  }
}
